    public ArrayList<Integer> solution(int[] a, int b) {
        int[] c = a.clone();
        ArrayList<Integer> an = new ArrayList<>();
        Arrays.sort(a);
        for (int i = 0; i < b; i++) {
                if (a[i] != c[i]) an.add(i+1);
        }
        return an;
    }
