public String solution(int[] a, int b) {
//        int[] arr = Arrays.stream(a).distinct().toArray();
Arrays.sort(a);
for (int i = 0; i < b-1; i++) {
if (a[i] == a[i+1]) return "D";
}
return "U";
}
